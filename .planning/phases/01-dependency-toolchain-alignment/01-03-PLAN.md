---
phase: 01-dependency-toolchain-alignment
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - docs/maintainers/dependency-upgrade-workflow.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - TOOL-03
must_haves:
  truths:
    - "Maintainers can follow one repeatable upgrade procedure from manifest edits to release prep."
    - "Upgrade execution requires explicit evidence capture (policy, build, compatibility)."
    - "Roadmap reflects finalized Plan 1 breakdown with execution order."
  artifacts:
    - path: "docs/maintainers/dependency-upgrade-workflow.md"
      provides: "Maintainer runbook for dependency upgrades"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 1 plan index and count"
  key_links:
    - from: "docs/maintainers/dependency-upgrade-workflow.md"
      to: "pnpm run deps:policy:check"
      via: "required upgrade sequence"
      pattern: "deps:policy:check"
    - from: ".planning/ROADMAP.md"
      to: "01-01-PLAN.md"
      via: "phase plan list"
      pattern: "Plans:"
---

<objective>
Document a repeatable dependency-upgrade workflow with explicit validation evidence and finalize Phase 1 roadmap plan metadata.

Purpose: Make future dependency updates auditable and low-risk while completing roadmap planning state.
Output: Maintainer upgrade runbook and updated roadmap plan list/count.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-dependency-toolchain-alignment/01-CONTEXT.md
@.planning/phases/01-dependency-toolchain-alignment/01-RESEARCH.md
@package.json
@.github/workflows/nodejs.yml
@scripts/check-dependency-policy.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write maintainer dependency-upgrade runbook with evidence contract</name>
  <read_first>
    - .planning/phases/01-dependency-toolchain-alignment/01-CONTEXT.md
    - .planning/phases/01-dependency-toolchain-alignment/01-RESEARCH.md
    - package.json
    - .github/workflows/nodejs.yml
  </read_first>
  <files>
    - docs/maintainers/dependency-upgrade-workflow.md
  </files>
  <action>
    Per D-08 and D-09, create `docs/maintainers/dependency-upgrade-workflow.md` with an ordered checklist that includes concrete commands and outputs: (1) update manifests to exact `4.0.0-beta.11` policy and shared peers, (2) run `pnpm install`, (3) run `pnpm run deps:policy:check`, (4) run `pnpm run build:ci`, (5) execute compatibility matrix checks for supported Angular majors, (6) capture evidence artifacts (command outputs, compatibility table, PR links), and (7) release-prep confirmation. Include a “Failure handling” section that blocks release when policy/build checks fail.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const p='docs/maintainers/dependency-upgrade-workflow.md';const s=fs.readFileSync(p,'utf8');const must=['pnpm run deps:policy:check','pnpm run build:ci','compatibility matrix','evidence'];for(const m of must){if(!s.toLowerCase().includes(m.toLowerCase())){console.error('missing:',m);process.exit(1)}}"</automated>
  </verify>
  <acceptance_criteria>
    - Runbook includes all ordered workflow steps from manifest updates through release prep.
    - Runbook includes explicit evidence artifacts to capture after execution.
    - Runbook contains release-blocking failure behavior for policy/build mismatch.
  </acceptance_criteria>
  <done>Maintainers have a concrete, auditable workflow for future dependency upgrades.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize Roadmap Phase 1 plan metadata</name>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/phases/01-dependency-toolchain-alignment/01-01-PLAN.md
    - .planning/phases/01-dependency-toolchain-alignment/01-02-PLAN.md
    - .planning/phases/01-dependency-toolchain-alignment/01-03-PLAN.md
  </read_first>
  <files>
    - .planning/ROADMAP.md
  </files>
  <action>
    Update Phase 1 in `.planning/ROADMAP.md` by replacing `**Plans**: TBD` with `**Plans**: 3 plans` and adding a `Plans:` checklist block:
    - `- [ ] 01-01-PLAN.md — Normalize dependency and peer policy manifests`
    - `- [ ] 01-02-PLAN.md — Add fail-fast dependency policy enforcement in scripts and CI`
    - `- [ ] 01-03-PLAN.md — Document upgrade workflow and evidence contract`
    Keep existing Goal/Depends on/Requirements unchanged.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('.planning/ROADMAP.md','utf8');const checks=['**Plans**: 3 plans','01-01-PLAN.md','01-02-PLAN.md','01-03-PLAN.md'];for(const c of checks){if(!s.includes(c)){console.error('missing:',c);process.exit(1)}}"</automated>
  </verify>
  <acceptance_criteria>
    - Phase 1 plans count is explicit (`3 plans`).
    - Roadmap includes all three Phase 1 PLAN filenames in checklist format.
  </acceptance_criteria>
  <done>Roadmap planning metadata is complete and matches generated plan files.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                        | Description                                               |
| ----------------------------------------------- | --------------------------------------------------------- |
| maintainer workflow execution → release process | Missing or weak documentation can cause unsafe upgrades   |
| roadmap metadata → execution orchestration      | Incorrect plan indexing can trigger wrong execution order |

## STRIDE Threat Register

| Threat ID | Category | Component                                       | Disposition | Mitigation Plan                                                                                    |
| --------- | -------- | ----------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------- |
| T-01-05   | R        | docs/maintainers/dependency-upgrade-workflow.md | mitigate    | Require explicit evidence artifacts and commands so upgrade actions are auditable and reproducible |
| T-01-06   | T        | .planning/ROADMAP.md                            | mitigate    | Pin exact PLAN filenames/count to prevent orchestration drift or skipped plans                     |

</threat_model>

<verification>
- Run the two task-level automated checks.
- Confirm `docs/maintainers/dependency-upgrade-workflow.md` exists and is linked in phase summary output.
</verification>

<success_criteria>

- Maintainer runbook exists with concrete, repeatable steps and evidence expectations.
- Roadmap Phase 1 reflects exact generated plan inventory.
  </success_criteria>

<output>
After completion, create `.planning/phases/01-dependency-toolchain-alignment/01-03-SUMMARY.md`
</output>
