---
phase: 01-dependency-toolchain-alignment
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - scripts/check-dependency-policy.mjs
  - package.json
  - .github/workflows/nodejs.yml
autonomous: true
requirements:
  - DEPS-02
  - DEPS-03
  - TOOL-01
must_haves:
  truths:
    - "CI fails fast when dependency policy drift is introduced."
    - "Policy checks run before expensive build jobs."
    - "Maintainers can execute the same dependency-policy gate locally and in CI."
  artifacts:
    - path: "scripts/check-dependency-policy.mjs"
      provides: "Deterministic repository-local policy validator"
    - path: "package.json"
      provides: "Root scripts entry for policy check"
    - path: ".github/workflows/nodejs.yml"
      provides: "Fail-fast CI gate ordering"
  key_links:
    - from: "package.json"
      to: "scripts/check-dependency-policy.mjs"
      via: "deps:policy:check script"
      pattern: "deps:policy:check"
    - from: ".github/workflows/nodejs.yml"
      to: "pnpm run deps:policy:check"
      via: "explicit workflow step before build:ci"
      pattern: "deps:policy:check"
---

<objective>
Add deterministic dependency-policy enforcement and wire it as a fail-fast CI gate.

Purpose: Prevent future drift from reintroducing mixed tsParticles versions, wrapper misalignment, or peer policy divergence.
Output: Policy checker script, root script command, and CI workflow integration before build.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-dependency-toolchain-alignment/01-CONTEXT.md
@.planning/phases/01-dependency-toolchain-alignment/01-RESEARCH.md
@package.json
@.github/workflows/nodejs.yml
@components/particles/projects/ng-particles/package.json
@components/confetti/projects/ng-confetti/package.json
@components/fireworks/projects/ng-fireworks/package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement repository-local dependency policy checker script</name>
  <read_first>
    - .planning/phases/01-dependency-toolchain-alignment/01-CONTEXT.md
    - .planning/phases/01-dependency-toolchain-alignment/01-RESEARCH.md
    - apps/angular-demo/package.json
    - apps/ionic-demo/package.json
    - components/particles/package.json
    - components/confetti/package.json
    - components/fireworks/package.json
    - components/particles/projects/ng-particles/package.json
    - components/confetti/projects/ng-confetti/package.json
    - components/fireworks/projects/ng-fireworks/package.json
  </read_first>
  <files>
    - scripts/check-dependency-policy.mjs
  </files>
  <behavior>
    - Test 1: Exits 0 when all listed manifests match exact tsParticles `4.0.0-beta.11`, aligned wrapper versions, and shared peer policy.
    - Test 2: Exits non-zero with file-prefixed violation messages when any tsParticles dependency/peer is not exact.
    - Test 3: Exits non-zero when publishable wrapper versions or Angular/RxJS peer ranges diverge from policy.
  </behavior>
  <action>
    Per D-06 and D-07, create `scripts/check-dependency-policy.mjs` that scans the explicit manifest list from Canonical References, validates: (a) all `tsparticles`/`@tsparticles/*` values are exact `4.0.0-beta.11` except workspace wrapper links, (b) publishable wrapper `version` fields are identical, and (c) publishable peers match `@angular/common`/`@angular/core` = `^20 || ^21`, `rxjs` = `^7.8.0`, wrapper tsParticles peers = exact `4.0.0-beta.11`. Print each failure as `path: rule: actual` and exit 1 if any violation exists.
  </action>
  <verify>
    <automated>node scripts/check-dependency-policy.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/check-dependency-policy.mjs` contains constants for manifest paths and expected policy values.
    - Script output includes file path prefixes for violations.
    - Running script on compliant manifests exits with code 0.
  </acceptance_criteria>
  <done>Policy checker deterministically enforces D-06 constraints from repository data alone.</done>
</task>

<task type="auto">
  <name>Task 2: Wire policy checker into root scripts and CI fail-fast sequence</name>
  <read_first>
    - package.json
    - .github/workflows/nodejs.yml
    - scripts/check-dependency-policy.mjs
  </read_first>
  <files>
    - package.json
    - .github/workflows/nodejs.yml
  </files>
  <action>
    Per D-06, D-07, and D-04, add root script `deps:policy:check` = `node scripts/check-dependency-policy.mjs`. Update `.github/workflows/nodejs.yml` in both `main` and `pr` jobs to use pnpm version `10.33.0` and add explicit `pnpm run deps:policy:check` step immediately before `pnpm run build:ci` (after install/formatting). Do not place this gate after build.
  </action>
  <verify>
    <automated>node scripts/check-dependency-policy.mjs && pnpm run deps:policy:check</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains `"deps:policy:check": "node scripts/check-dependency-policy.mjs"`.
    - `.github/workflows/nodejs.yml` has `version: 10.33.0` for both pnpm setup steps.
    - `.github/workflows/nodejs.yml` contains `pnpm run deps:policy:check` before each `pnpm run build:ci` step.
  </acceptance_criteria>
  <done>Local and CI policy enforcement run from one shared command and fail before build when drift appears.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                             | Description                                                    |
| ------------------------------------ | -------------------------------------------------------------- |
| contributor commits → CI policy gate | Incorrect dependency edits attempt to pass pipeline            |
| script logic → release decisions     | Faulty validation logic can allow incompatible dependency sets |

## STRIDE Threat Register

| Threat ID | Category | Component                           | Disposition | Mitigation Plan                                                                                            |
| --------- | -------- | ----------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------- |
| T-01-03   | T        | scripts/check-dependency-policy.mjs | mitigate    | Use explicit manifest allowlist + exact expected values; non-compliant values trigger non-zero exit        |
| T-01-04   | D        | .github/workflows/nodejs.yml        | mitigate    | Run policy gate before build to prevent costly CI runs and release progression on invalid dependency state |

</threat_model>

<verification>
- Run `pnpm run deps:policy:check` locally.
- Run `pnpm run build:ci` once after checker passes.
</verification>

<success_criteria>

- Any dependency-policy drift now fails via `deps:policy:check` locally and in CI.
- CI gate order is install → formatting → deps policy check → build.
  </success_criteria>

<output>
After completion, create `.planning/phases/01-dependency-toolchain-alignment/01-02-SUMMARY.md`
</output>
