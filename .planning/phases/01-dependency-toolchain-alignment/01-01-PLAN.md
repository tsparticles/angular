---
phase: 01-dependency-toolchain-alignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/angular-demo/package.json
  - apps/ionic-demo/package.json
  - components/particles/package.json
  - components/confetti/package.json
  - components/fireworks/package.json
  - components/particles/projects/ng-particles/package.json
  - components/confetti/projects/ng-confetti/package.json
  - components/fireworks/projects/ng-fireworks/package.json
autonomous: true
requirements:
  - DEPS-01
  - DEPS-02
  - DEPS-03
  - TOOL-01
  - TOOL-02
must_haves:
  truths:
    - "Maintainers see one tsParticles runtime version policy (4.0.0-beta.11) in all scoped manifests."
    - "Publishable wrapper packages expose one shared Angular peer compatibility policy."
    - "Wrapper package release versions remain explicitly aligned."
  artifacts:
    - path: "apps/angular-demo/package.json"
      provides: "Consumer dependency policy baseline"
    - path: "apps/ionic-demo/package.json"
      provides: "Ionic dependency policy baseline"
    - path: "components/*/projects/*/package.json"
      provides: "Publishable peer compatibility policy"
  key_links:
    - from: "apps/*/package.json"
      to: "components/*/package.json"
      via: "@tsparticles/* and wrapper version alignment"
      pattern: "4.0.0-beta.11"
    - from: "components/*/projects/*/package.json"
      to: "Phase compatibility policy"
      via: "peerDependencies ranges"
      pattern: "\^20 \|\| \^21"
---

<objective>
Normalize workspace package manifests so Phase 1 has one deterministic dependency and peer policy.

Purpose: Remove dependency/peer drift before policy automation is wired.
Output: Updated app, wrapper, and publishable package manifests aligned to Phase 1 decisions.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-dependency-toolchain-alignment/01-CONTEXT.md
@.planning/phases/01-dependency-toolchain-alignment/01-RESEARCH.md
@apps/angular-demo/package.json
@apps/ionic-demo/package.json
@components/particles/package.json
@components/confetti/package.json
@components/fireworks/package.json
@components/particles/projects/ng-particles/package.json
@components/confetti/projects/ng-confetti/package.json
@components/fireworks/projects/ng-fireworks/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normalize tsParticles runtime specifiers across all targeted manifests</name>
  <read_first>
    - apps/angular-demo/package.json
    - apps/ionic-demo/package.json
    - components/particles/package.json
    - components/confetti/package.json
    - components/fireworks/package.json
    - .planning/phases/01-dependency-toolchain-alignment/01-CONTEXT.md
  </read_first>
  <files>
    - apps/angular-demo/package.json
    - apps/ionic-demo/package.json
    - components/particles/package.json
    - components/confetti/package.json
    - components/fireworks/package.json
  </files>
  <action>
    Per D-01 and D-02, update all `@tsparticles/*` runtime dependency entries and `tsparticles` entries in the listed manifests to exact `4.0.0-beta.11` (remove caret prerelease forms such as `^4.0.0-beta.11`). Keep wrapper workspace dependencies (`@tsparticles/angular`, `angular-confetti`, `angular-fireworks`) as `workspace:^` where already used. Do not introduce mixed prerelease specifier styles.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const files=['apps/angular-demo/package.json','apps/ionic-demo/package.json','components/particles/package.json','components/confetti/package.json','components/fireworks/package.json'];let bad=[];for(const f of files){const p=JSON.parse(fs.readFileSync(f,'utf8'));for(const sec of ['dependencies','peerDependencies']){const o=p[sec]||{};for(const [k,v] of Object.entries(o)){if((k==='tsparticles'||k.startsWith('@tsparticles/'))&&v!=='4.0.0-beta.11'&&v!=='workspace:^')bad.push(`${f}:${sec}:${k}=${v}`);}}}if(bad.length){console.error(bad.join('\n'));process.exit(1)}"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/angular-demo/package.json` contains no `"@tsparticles/` dependency with a `^` prefix.
    - `apps/ionic-demo/package.json` contains no `"@tsparticles/` dependency with a `^` prefix.
    - All updated tsParticles entries in touched files resolve to `4.0.0-beta.11` exactly.
  </acceptance_criteria>
  <done>All workspace package-level runtime dependencies use one exact tsParticles beta line with no mixed prerelease specifier style.</done>
</task>

<task type="auto">
  <name>Task 2: Align publishable wrapper versions and shared peer compatibility ranges</name>
  <read_first>
    - components/particles/projects/ng-particles/package.json
    - components/confetti/projects/ng-confetti/package.json
    - components/fireworks/projects/ng-fireworks/package.json
    - .planning/phases/01-dependency-toolchain-alignment/01-CONTEXT.md
  </read_first>
  <files>
    - components/particles/projects/ng-particles/package.json
    - components/confetti/projects/ng-confetti/package.json
    - components/fireworks/projects/ng-fireworks/package.json
  </files>
  <action>
    Per D-03, D-04, and D-05, keep all three publishable wrappers on the same `version` value and set one explicit peer policy: `@angular/common` and `@angular/core` to `^20 || ^21`, `rxjs` to `^7.8.0`, and wrapper-specific tsParticles peers to exact `4.0.0-beta.11` (including `@tsparticles/engine` in `ng-confetti`). Remove overly broad legacy peer ranges like `>=2.0.0` and mismatched peer specifiers.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const files=['components/particles/projects/ng-particles/package.json','components/confetti/projects/ng-confetti/package.json','components/fireworks/projects/ng-fireworks/package.json'];const pkgs=files.map(f=>({f,p:JSON.parse(fs.readFileSync(f,'utf8'))}));const versions=[...new Set(pkgs.map(x=>x.p.version))];if(versions.length!==1){console.error('wrapper version mismatch',versions);process.exit(1)}for(const {f,p} of pkgs){const peer=p.peerDependencies||{};if(peer['@angular/common']!=='^20 || ^21'||peer['@angular/core']!=='^20 || ^21'||peer['rxjs']!=='^7.8.0'){console.error('peer policy mismatch in',f,peer);process.exit(1)}for(const [k,v] of Object.entries(peer)){if(k.startsWith('@tsparticles/')&&v!=='4.0.0-beta.11'){console.error('tsParticles peer mismatch',f,k,v);process.exit(1)}}}"</automated>
  </verify>
  <acceptance_criteria>
    - All three publishable wrapper manifests expose identical `version` values.
    - Each publishable wrapper has `@angular/common: ^20 || ^21`, `@angular/core: ^20 || ^21`, and `rxjs: ^7.8.0` in `peerDependencies`.
    - Each publishable wrapper tsParticles peer entry equals `4.0.0-beta.11` exactly.
  </acceptance_criteria>
  <done>Publishable wrapper manifests enforce one explicit release-train + peer compatibility contract.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                             | Description                                                                         |
| ------------------------------------ | ----------------------------------------------------------------------------------- |
| maintainer edits → package manifests | Incorrect version edits can bypass policy intent and ship incompatible dependencies |
| package metadata → consumers         | Invalid peer ranges can break install/resolve behavior for downstream apps          |

## STRIDE Threat Register

| Threat ID | Category | Component                 | Disposition | Mitigation Plan                                                                                     |
| --------- | -------- | ------------------------- | ----------- | --------------------------------------------------------------------------------------------------- |
| T-01-01   | T        | package.json manifests    | mitigate    | Pin exact tsParticles values and normalize peer ranges per D-01/D-05; reject broad ambiguous ranges |
| T-01-02   | D        | publishable peer metadata | mitigate    | Align wrapper versions and shared peer policy so incompatible installs are prevented early          |

</threat_model>

<verification>
- Run both task-level Node verification commands.
- Run `pnpm install --frozen-lockfile` and ensure no manifest parse errors after edits.
</verification>

<success_criteria>

- No mixed prerelease tsParticles specifiers remain in targeted manifests.
- Wrapper publishable manifests expose one shared Angular/RxJS/tsParticles peer contract.
  </success_criteria>

<output>
After completion, create `.planning/phases/01-dependency-toolchain-alignment/01-01-SUMMARY.md`
</output>
